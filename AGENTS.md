# AGENTS.md

Compact guidance for OpenCode sessions working in this repo.

## What this repo is

`create-react-native-tui` (`react-native-tui`) — an interactive CLI that scaffolds React Native projects from a prebuilt Clean Architecture template. The CLI itself is the product published to npm. It is NOT a React Native app; it is a Node/Bun CLI written in TypeScript with an Ink (React) UI.

The generated project comes from a separate GitHub repo: `alejandro-technology/react-native-template` branch `main` (hardcoded in `src/infrastructure/template/github-template.repository.ts`). This repo does not contain the template source.

## Toolchain

- **Bun is the primary tool.** `bun run dev` runs the CLI directly from TSX source (`src/ui/tui.tsx`). `bun run build` produces the publishable bundle.
- `bun run check` runs `tsc --noEmit` for type checking. The build step (`bun run build`) uses `Bun.build` and does NOT type-check, so `check` and `build` are independent — a green build does not imply a green `check`.
- Build (`scripts/build.ts`) uses `Bun.build` with `target: "node"`, `minify: true`, single output `bin/index.js`. The build script prepends the `#!/usr/bin/env node` shebang and `chmod 755`s the file.
- **`bin/` is a build artifact and is gitignored.** It is regenerated by `prepublishOnly` before every `npm publish`. Do not edit `bin/index.js` by hand and do not commit it. The published package ships ONLY `bin/` (`package.json#files`).
- Three lockfiles exist (`bun.lockb`, `package-lock.json`, `pnpm-lock.yaml`). Pick Bun for CLI dev work unless explicitly asked otherwise.
- `tsconfig.json` has `strict: false`, `moduleResolution: "bundler"`, `target/module: ESNext`. Source imports use `.js` extensions in relative paths (ESM). Do not drop them.
- Node engine in `package.json` is `>= 22.0.0`; the README says `>= 18` but is stale. Trust `package.json`.

## Lint & format

- **ESLint 9 flat config** at `eslint.config.js`. Uses `typescript-eslint` (recommended), `@eslint/js`, `globals` (Node), and `eslint-config-prettier` last (to disable stylistic rules).
- **Prettier 3** at `.prettierrc.json` (printWidth 100, double quotes, semicolons, trailing commas all, LF). `.prettierignore` excludes `bin/`, `node_modules/`, lockfiles, `.codegraph/`.
- **Husky 9** manages the git hook. `core.hooksPath` is set to `.husky/_`; user hooks live in `.husky/` (e.g. `pre-commit`). The `prepare` script in `package.json` runs `husky` after `bun install` to set up hooks on fresh clones.
- **lint-staged** is configured in `package.json#lint-staged`: on staged `*.{ts,tsx,js,jsx}` runs `eslint --fix` then `prettier --write`; on staged `*.{json,md,mdx,yml,yaml}` runs `prettier --write`. Commits abort if any task exits non-zero, so the working tree must lint+format clean before commit.
- Override in `eslint.config.js` disables `@typescript-eslint/no-explicit-any` for `src/ui/prompts/main.prompt.ts` (enquirer's `Form`/`multiselect` types don't match the runtime contract).

## Architecture (Clean Architecture, 4 layers)

```
src/
  domain/          models, validators, constants, repository interfaces
  application/
    scaffold/      orchestrator (scaffold.use-case.ts) + steps/ (9 step files) + backend-modules.registry.ts
    commands/      run-command.use-case.ts, exec-streamed.ts, progress.ts
  infrastructure/  GithubTemplateRepository (download/extract tar from GitHub)
  ui/
    tui.tsx        entrypoint → Routes to CLI or TUI mode
    cli/           cli.ts, cli-parsers.ts, cli-logger.ts (headless CLI mode adapters)
    prompts/       enquirer prompt chains (TUI)
    components/     Ink primitives (ProgressBar, StepList, Badge, etc.) (TUI)
    commands/      scaffold.handler.ts (download + scaffold + cleanup flow, shared by CLI/TUI)
    content/       version.ts, help.ts (static command output strings, shared by CLI/TUI)
    hooks/         use-output-buffer.ts (ring buffer for streaming output)
```

`scaffoldProject` is an orchestrator that calls 9 step functions in sequence (`steps/`), each receiving a shared `ScaffoldContext`. Firebase module definitions (file paths, DI regex patches, npm deps) live in `backend-modules.registry.ts` — the single source of truth for adding/removing backend modules.

The entrypoint `tui.tsx` is dual-mode. If called with CLI subcommands (`react-native-tui scaffold ...`), it delegates to `src/ui/cli/cli.ts` for headless execution. If called with no arguments, it starts the interactive `runPrompt()` and renders `<App>` (Ink). Both modes share `scaffold.handler.ts` and the use-cases.

## Critical quirks (easy to get wrong)

- **`run-command.use-case.ts` hardcodes `npm run <script>`** for clean/pod-install/run-android, regardless of the package manager the user selected in the prompt. The `execStreamed` calls all pass `"npm"` directly. If you "fix" this to respect `PM_COMMANDS`, you are changing behavior — confirm with the user first.
- **Two sources of truth for RN version.** `RN_VERSION` in `src/domain/constants.ts` is shown only in `version` output. The actual init pin `"0.83.4"` lives in `src/application/scaffold/steps/init-react-native.step.ts`. When bumping the RN version, update both sites — they do not auto-sync. `VERSION` in `constants.ts` and `package.json#version` are currently in sync (`1.1.0`), but there is no mechanism enforcing this.
- **Regex patches are line-shape-sensitive.** `patch-di-injectors.step.ts` and `prune-backend-modules.step.ts` apply hardcoded regex (`FIREBASE_MODULES` entries) to patch DI files and remove Firebase imports. Changing formatting in the template (extra blank lines, different indentation) will silently break the pruning. The regex shapes live in `backend-modules.registry.ts`.
- Agent-config folders (`.claude`, `.opencode`, `.trae`) are copied from a `.ai/` folder in the template, not from a per-agent source. See `CODE_AGENT_FILES` in `scaffold.use-case.ts`.
- **Ink `<Text color={}>` expects string color names** (`"yellow"`, `"green"`, `"red"`), NOT chalk functions (`chalk.yellow`). Passing a chalk function causes a runtime crash (`color.startsWith is not a function`).
- **Execa v9+ exports `Subprocess`**, not `ExecaChildProcess`. Importing `ExecaChildProcess` will fail `tsc --noEmit`.

## Tests

- **There are no tests in this repo.** `npm test` runs `jest` but there is no jest config and no `*.test.ts` files. Do not assume a test suite, fixtures, or coverage gate exists. Verify before claiming "tests pass."
- There is no CI, no husky, no lint/format script, no codegen step in this repo.

## Commands

| Command                | What it does (in THIS repo)                                                                 |
| ---------------------- | ------------------------------------------------------------------------------------------- |
| `bun run dev`          | Run the CLI interactively from source (TSX). Main loop for manual QA.                       |
| `bun run check`        | `tsc --noEmit` — type checking only, no output. Independent from build.                     |
| `bun run build`        | Build `bin/index.js` via `scripts/build.ts`. Required before publish.                       |
| `bun run lint`         | Run ESLint over the repo. Non-zero exit on any warning or error.                            |
| `bun run lint:fix`     | Same as `lint` but applies auto-fixes.                                                      |
| `bun run format`       | Run Prettier in write mode across the repo.                                                 |
| `bun run format:check` | Run Prettier in check mode (CI-friendly).                                                   |
| `npm publish`          | Publish to npm. `prepublishOnly` runs `bun run build` automatically.                        |
| `bun run clean`        | `rm -rf bin/*` — wipes the BUILD OUTPUT, not caches. (Not the user-facing `clean` command.) |

A commit triggers `.husky/pre-commit` → `bunx lint-staged` → only staged files are processed.

To exercise the real CLI without `bun run dev`, install built artifact: `node bin/index.js` (or `npm link`).

## Conventions

- Conventional Commits, lowercase types (`feat:`, `chore:`, `refactor:`, `docs:`). Default branch is `main`.
- No AI/"Co-Authored-By" attribution in commits.
- Source comments and UI strings are English.
